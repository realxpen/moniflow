import { PlaceholderScreen } from "@/components/ui";

export default function IdentityScreen() {
  return (
    <PlaceholderScreen
      description="Identity collection is intentionally deferred. Phase 2 will use only fields required by current BMONI sandbox documentation."
      eyebrow="IDENTITY · PLACEHOLDER"
      nextHref="/onboarding/wallet"
      title="First, let’s know you."
    />
  );
}
