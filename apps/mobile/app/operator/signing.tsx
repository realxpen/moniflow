import { PlaceholderScreen } from "@/components/ui";

export default function SigningScreen() {
  return (
    <PlaceholderScreen
      description="Future signing remains on-device. No private key, signing payload, SDK, or BMONI method exists in this scaffold."
      eyebrow="DEVICE SIGNING · PLACEHOLDER"
      nextHref="/operator/result"
      title="Your device. Your authorization."
    />
  );
}
