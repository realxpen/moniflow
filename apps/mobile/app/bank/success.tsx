import { PlaceholderScreen } from "@/components/ui";

export default function BankSuccessScreen() {
  return (
    <PlaceholderScreen
      description="No bank account has been saved or verified during Phase 1. This is a route and layout placeholder only."
      eyebrow="BANK RESULT · PREVIEW"
      nextHref="/(tabs)/home"
      nextLabel="Return home"
      title="Destination flow reserved."
    />
  );
}
