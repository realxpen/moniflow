import { PlaceholderScreen } from "@/components/ui";

export default function ProfileScreen() {
  return (
    <PlaceholderScreen
      description="Account, sandbox status, saved banks, and security settings will appear here as their real foundations are implemented."
      eyebrow="PROFILE · PLACEHOLDER"
      title="Your MONIFlow"
    />
  );
}
