import { PlaceholderScreen } from "@/components/ui";

export default function PocketsScreen() {
  return (
    <PlaceholderScreen
      description="Pockets will be application-level bookkeeping unless BMONI explicitly supports provider-held partitions."
      eyebrow="CALM MODE · PLACEHOLDER"
      title="Money Pockets"
    />
  );
}
